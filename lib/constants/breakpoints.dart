/// Minimum width (logical pixels) at which the app switches from
/// bottom navigation to side navigation / multi-column layouts.
const double kNavBreakpoint = 600;

/// Minimum width (logical pixels) per tile in masonry grid layouts.
const double kTileMinWidth = 600;
