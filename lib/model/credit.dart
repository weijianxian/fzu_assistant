class CreditStatistics {
  final String type;  // 学分类型
  final String gain;  // 已获得
  final String total; // 应获学分

  const CreditStatistics({
    required this.type,
    required this.gain,
    required this.total,
  });
}
